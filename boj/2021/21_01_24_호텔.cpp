#include <string>
#include <vector>
#include <set>
#include <hash_set>

#define ll long long

using namespace std;

vector<long long> solution(long long k, vector<long long> room_number)
{

    vector<long long> answer;
    return answer;
}